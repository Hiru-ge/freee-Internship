<div class="shift-page-container <%= @is_owner ? 'owner-mode' : 'employee-mode' %>">
  <h1>シフトページ</h1>


  <!-- シフト表 -->
  <div class="shift-calendar-section">
    <h2>シフト表</h2>
    <p>交代を依頼したい<strong>自分のシフト</strong>をクリックすると、依頼フォームへ遷移します。<br>
    <small class="help-text">※ 黄色で表示されているシフトがクリック可能です</small></p>

    <!-- シフト管理ボタン -->
    <div class="shift-management">
      <button class="button" data-action="request-list">自分へのリクエスト一覧</button>
      <button class="button" data-action="deletion-form">欠勤申請</button>
      <button id="shift-add-btn" class="button owner-only hidden" data-action="shift-add">シフト追加依頼</button>
    </div>

    <div class="month-navigation">
      <h3 id="calendar-title"></h3>
      <div class="month-navigation-buttons">
        <button class="button small-button" data-action="prev-week"><</button>
        <button class="button small-button" data-action="next-week">></button>
      </div>
    </div>

    <div id="shift-calendar-container">
      <p>シフト情報を読み込んでいます...</p>
    </div>
  </div>

  <!-- 給与ゲージ（オーナーモード） -->
  <% if @is_owner %>
    <div class="wage-gauge-section">
      <h2>従業員給与状況</h2>
      <table class="employee-wage-table">
      <thead>
        <tr>
          <th>従業員名</th>
            <th>給与状況</th>
        </tr>
      </thead>
      <tbody id="employee-list-body">
        <tr><td colspan="2">読み込み中...</td></tr>
      </tbody>
    </table>
  </div>
  <% else %>
    <!-- 給与ゲージ（従業員モード） -->
    <div class="wage-gauge-section">
      <h2>給与状況</h2>
      <div id="personal-wage-gauge" class="wage-gauge">
      <div class="gauge-container">
        <div class="gauge-bar">
          <div class="gauge-fill" style="width: 0%"></div>
        </div>
        <div class="gauge-text">読み込み中...</div>
      </div>
    </div>
  </div>
  <% end %>

  <!-- メッセージ表示 -->
  <div id="message" class="message hidden"></div>
</div>

