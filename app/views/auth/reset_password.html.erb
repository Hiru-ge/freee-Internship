<div class="form-container">
  <h1>パスワード再設定</h1>
  <p class="form-description">認証コードを正しく認識できました。新しいパスワードを設定してください。</p>

  <%= form_with url: password_reset_path, method: :post, local: true, id: "password-reset-form" do |form| %>
    <%= form.hidden_field :employee_id, value: @employee_id %>
    <%= form.hidden_field :code, value: @verification_code %>

    <div class="form-group">
      <div class="form-row">
        <label for="new-password" class="form-label">新しいパスワード:</label>
        <%= form.password_field :new_password,
            placeholder: "新しいパスワードを入力",
            required: true,
            id: "new-password",
            class: "form-input" %>
      </div>
      <div class="form-help">
        <small>パスワード要件:</small>
        <ul>
          <li>8文字以上</li>
          <li>英字と数字を含む</li>
          <li>特殊文字を含む（推奨）</li>
        </ul>
      </div>
    </div>

    <div class="form-group">
      <div class="form-row">
        <label for="confirm-password" class="form-label">パスワード（確認）:</label>
        <%= form.password_field :confirm_password,
            placeholder: "パスワードを再入力",
            required: true,
            id: "confirm-password",
            class: "form-input" %>
      </div>
    </div>

    <div class="form-buttons">
      <%= form.submit "パスワードを再設定", class: "form-button" %>
      <%= link_to "戻る", verify_password_reset_auth_path(employee_id: @employee_id), class: "form-button secondary" %>
    </div>
  <% end %>

  <div class="login-links">
    <%= link_to "ログイン画面に戻る", login_path, class: "link" %>
  </div>
</div>

<%= javascript_include_tag "auth" %>
