<div class="container shift-exchange-form-page">
  <h1>シフト交代依頼</h1>

  <div class="form-container">
    <div class="form-info">
      <h2>依頼内容</h2>
      <div class="info-grid">
        <div class="info-item">
          <label>日付:</label>
          <span><%= @date %></span>
        </div>
        <div class="info-item">
          <label>時間:</label>
          <span><%= @start_time %> - <%= @end_time %></span>
        </div>
        <div class="info-item">
          <label>申請者:</label>
          <span><%= @applicant_name %></span>
        </div>
      </div>
    </div>

    <form id="request-form" action="<%= shift_exchanges_path %>" method="post">
      <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
      <%= hidden_field_tag "shift_exchange[applicant_id]", @applicant_id %>
      <%= hidden_field_tag "shift_exchange[date]", @date %>
      <%= hidden_field_tag "shift_exchange[start_time]", @start_time %>
      <%= hidden_field_tag "shift_exchange[end_time]", @end_time %>

      <div class="form-section">
        <h3>交代相手の選択</h3>
        <p>交代を依頼したい従業員を選択してください。</p>

        <div class="employee-selection">
          <select id="employee-select" name="shift_exchange[employee_id]" required>
            <option value="">従業員を選択してください</option>
          </select>
        </div>
      </div>

      <div class="form-section">
        <h3>メッセージ（任意）</h3>
        <textarea name="shift_exchange[message]" placeholder="交代依頼の理由やメッセージを入力してください"></textarea>
      </div>

      <div class="form-buttons">
        <button type="submit" class="form-button">この内容でリクエストを送信</button>
        <a href="#" data-action="go-back" class="form-button secondary">シフトページに戻る</a>
      </div>
    </form>
  </div>

</div>
